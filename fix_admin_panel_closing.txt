// Fixed Game Mechanics Section - Lines 823-850
                        </tbody>
                    </table>
                </div>
            </div>

            <div className="text-xs text-gray-600 px-1 mt-2">
                * Check to enable feature. "Setup" opens configuration.
            </div>
        </fieldset>
    );
}

// Modals are rendered in the main AdminPanel component, not here
function BossEditorModal({ onClose }) {
